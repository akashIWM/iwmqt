# This project has two separate .env files, one per package:
#   server/.env  — copy from server/.env.example
#   client/.env  — copy from client/.env.example
#
# See those files for the actual variables each part of the app needs.
